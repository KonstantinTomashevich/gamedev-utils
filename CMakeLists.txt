project (SimpleCardGame)
cmake_minimum_required (VERSION 2.8.6)
enable_testing ()

include_directories (${CMAKE_CURRENT_SOURCE_DIR})
add_subdirectory (QuestStateMachine)
add_subdirectory (SharedPointer)
